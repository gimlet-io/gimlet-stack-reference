{{ if .aws.efsSupport -}}
---
apiVersion: identity.aws.crossplane.io/v1beta1
kind: IAMRole
metadata:
  name: efs-csi-driver-role
spec:
  forProvider:
    assumeRolePolicyDocument: |
      {
          "Version": "2012-10-17",
          "Statement": [
              {
                  "Effect": "Allow",
                  "Principal": {
                      "Federated": "arn:aws:iam::{{ .aws.accountID -}}:oidc-provider/oidc.eks.{{ .aws.region -}}.amazonaws.com/id/{{ .aws.oidcProviderID -}}"
                  },
                  "Action": "sts:AssumeRoleWithWebIdentity",
                  "Condition": {
                      "StringEquals": {
                      "oidc.eks.{{ .aws.region -}}.amazonaws.com/id/{{ .aws.oidcProviderID -}}:sub" : "system:serviceaccount:kube-system:efs-csi-controller-sa"
                      }
                  }
              }
          ]
      }
  providerConfigRef:
    name: default
---
apiVersion: identity.aws.crossplane.io/v1beta1
kind: IAMRolePolicyAttachment
metadata:
  name: efs-csi-driver-role-policy-assignment
spec:
  forProvider:
    policyArn: arn:aws:iam::{{ .aws.accountID -}}:policy/efs-csi-driver-policy
    roleName: efs-csi-driver-role
{{- end }}