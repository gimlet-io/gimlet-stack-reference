{{ if .aws.clusterAutoscaler -}}
---
apiVersion: identity.aws.crossplane.io/v1beta1
kind: IAMRole
metadata:
  name: eks-autoscaler-role
spec:
  forProvider:
    assumeRolePolicyDocument: |
      {
          "Version": "2012-10-17",
          "Statement": [
              {
                  "Effect": "Allow",
                  "Principal": {
                      "Federated": "arn:aws:iam::<<OIDC provider id>>:oidc-provider/oidc.eks.<<TODO region>>.amazonaws.com/id/<<OIDC provider id>>"
                  },
                  "Action": "sts:AssumeRoleWithWebIdentity",
                  "Condition": {
                      "StringEquals": {
                      "oidc.eks.<<TODO region>>.amazonaws.com/id/<<OIDC provider id>>:sub" : "system:serviceaccount:kube-system:cluster-autoscaler"
                      }
                  }
              }
          ]
      }
  providerConfigRef:
    name: default
---
apiVersion: identity.aws.crossplane.io/v1beta1
kind: IAMRolePolicyAttachment
metadata:
  name: eks-autoscaler-role-policy-assignment
spec:
  forProvider:
    policyArn: arn:aws:iam::<<account id>>:policy/eks-autoscaler-policy
    roleName: eks-autoscaler-role
{{- end }}